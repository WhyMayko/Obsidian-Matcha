# Community Configs

Place community config `.txt` files here.
Each file is a JSON object saved by SaveManager.

To load from Matcha console (with script running):
```lua
community.loadConfig("ConfigName")
```
